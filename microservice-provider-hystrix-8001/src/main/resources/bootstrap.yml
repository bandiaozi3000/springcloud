spring:
  profiles:
    active: dev
  devtools:
    restart:
      enabled: true
      additional-paths: src/main/java

logging:
  config: classpath:logback-spring.xml
  log:
    level: info

#management:
#  endpoints:
#    web:
#      exposure:
#        #暴露hystrix监控端点
#        include: 'hystrix.stream'








